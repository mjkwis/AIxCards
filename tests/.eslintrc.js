module.exports = {
  rules: {
    // Allow console statements in tests for debugging
    "no-console": "off",
    // React hooks rules don't apply to test fixtures
    "react-hooks/rules-of-hooks": "off",
  },
};

