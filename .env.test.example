# Test Environment Configuration
# This file is used for E2E tests (Playwright)
# Copy this to .env.test and fill in your test values

# Site URL
SITE_URL=http://localhost:3000

# Supabase Configuration (Test Environment)
# You can use the same Supabase instance as development, or create a separate test project
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_anon_key_here

# Service Role Key (only needed for test cleanup/setup scripts)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# OpenRouter Configuration (for AI generation tests)
# For tests, you can use a mock key or real key with cheap model
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=openai/gpt-4o-mini

# E2E Test User Credentials
# These are automatically generated during tests, but can be overridden
E2E_USERNAME=test@example.com
E2E_PASSWORD=TestPassword123!